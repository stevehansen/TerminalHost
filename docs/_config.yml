title: TerminalHost
description: Cross-platform desktop app (Windows/macOS) that manages per-project terminal pairs (AI assistant + shell) with a dedicated Run terminal.
theme: minima

url: "https://stevehansen.github.io"
baseurl: "/TerminalHost"

# Only show these pages in the header navigation (prevents specs from appearing)
header_pages:
  - index.md
  - getting-started.md
  - features.md
  - feature-tour.md
  - usage.md
  - shortcuts.md
  - developer.md

minima:
  skin: auto

github_username: stevehansen
