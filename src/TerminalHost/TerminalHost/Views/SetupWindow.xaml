<Window x:Class="TerminalHost.Views.SetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:TerminalHost.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModels:SetupViewModel, IsDesignTimeCreatable=True}"
        Title="TerminalHost :: Setup" Height="640" Width="800"
        Background="{StaticResource BackgroundMediumBrush}" Foreground="{StaticResource TextBrightBrush}"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BackgroundHoverBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrightBrush}"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderDefaultBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BackgroundActiveBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LinkButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="TerminalHost Setup" FontSize="{StaticResource FontSizeTitle}" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBlock Text="Checking for recommended tools and dependencies." Foreground="{StaticResource TextDimmedBrush}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Dependencies}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <Border BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="0,0,0,1" Padding="10" Margin="0,5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <Button Style="{StaticResource LinkButton}"
                                                Command="{Binding DataContext.ToggleDetailsCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="{StaticResource FontSizeLarge}"/>
                                                <TextBlock Text="{Binding Description}" Foreground="{StaticResource TextLightBrush}" Margin="0,2,0,0"/>
                                                <TextBlock Text="{Binding DetectedVersion}" Foreground="{StaticResource TextDimmedBrush}" FontSize="{StaticResource FontSizeTiny}" Margin="0,5,0,0"/>
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock VerticalAlignment="Center" Margin="0,0,20,0" FontSize="{StaticResource FontSizeNormal}" ToolTip="Detection Status">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="..."/>
                                                    <Setter Property="Foreground" Value="{StaticResource TextDimmedBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsDetecting}" Value="True">
                                                            <Setter Property="Text" Value="◌"/>
                                                            <Setter Property="Foreground" Value="{StaticResource SyntaxLightBlueBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsDetecting}" Value="False">
                                                            <Setter Property="Text" Value="●"/>
                                                            <Setter Property="Foreground" Value="{StaticResource SyntaxGreenBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsInstalled}" Value="False">
                                                            <Setter Property="Text" Value="●"/>
                                                            <Setter Property="Foreground" Value="{StaticResource StatusErrorBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                        <Button Content="Download"
                                                Command="{Binding DataContext.OpenInstallLinkCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                Margin="0,0,8,0"
                                                Visibility="{Binding InstallUrl, Converter={StaticResource NullToCollapsedConverter}}">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                    <Setter Property="ToolTip" Value="{Binding InstallUrl}"/>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    
                                                                                <Button Content="Install"
                                                                                        Command="{Binding DataContext.InstallDependencyCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                                        CommandParameter="{Binding}"
                                                                                        IsEnabled="{Binding IsNotInstalling}"
                                                                                        ToolTip="{Binding InstallToolTipText}">
                                                                                    <Button.Style>
                                                                                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                                                            <Style.Triggers>
                                                                                                <DataTrigger Binding="{Binding IsInstalled}" Value="True">
                                                                                                    <Setter Property="Content" Value="Homepage"/>
                                                                                                </DataTrigger>
                                                                                                <DataTrigger Binding="{Binding InstallCommand}" Value="{x:Null}">
                                                                                                    <Setter Property="Content" Value="Homepage"/>
                                                                                                </DataTrigger>
                                                                                            </Style.Triggers>
                                                                                        </Style>
                                                                                    </Button.Style>
                                                                                </Button>
                                                                            </StackPanel>                                </Grid>
                            </Border>

                            <Border x:Name="DetailsPanel" Background="{StaticResource BackgroundDarkBrush}" Margin="20,0,20,10" Padding="10" CornerRadius="3">
                                <StackPanel>
                                    <TextBlock Text="Debug Info" FontWeight="Bold" Foreground="{StaticResource TextDimmedBrush}"/>
                                    <Grid Margin="0,5,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Exit Code: " Foreground="{StaticResource TextDimmedBrush}"/>
                                        <TextBlock Grid.Column="1" Text="{Binding ExitCode}" Foreground="{StaticResource TextDimmedBrush}"/>
                                    </Grid>
                                    <TextBlock Text="Output:" Margin="0,5,0,2" Foreground="{StaticResource TextDimmedBrush}"/>
                                    <TextBox Text="{Binding FullOutput}"
                                             IsReadOnly="True"
                                             MaxHeight="150"
                                             VerticalScrollBarVisibility="Auto"
                                             Background="{StaticResource BackgroundDarkestBrush}"
                                             Foreground="{StaticResource TextLightBrush}"
                                             BorderThickness="0"/>
                                </StackPanel>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ShowDetails}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                            </Border>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Refresh All" Command="{Binding CheckAllDependenciesCommand}"/>
            <Button x:Name="ContinueButton" Content="Continue to App" Click="ContinueButton_Click" Margin="10,0,0,0" FontWeight="Bold" Background="{StaticResource StatusSuccessBrush}" IsDefault="True"/>
            <Button Content="Close" Click="CloseButton_Click" Margin="10,0,0,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
