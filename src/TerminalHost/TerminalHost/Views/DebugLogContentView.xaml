<UserControl x:Class="TerminalHost.Views.DebugLogContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:TerminalHost.ViewModels"
             xmlns:core="clr-namespace:TerminalHost.Core.Interfaces;assembly=TerminalHost.Core"
             d:DataContext="{d:DesignInstance vm:DebugLogViewModel}"
             mc:Ignorable="d"
             Background="{StaticResource BackgroundDarkestBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="{StaticResource BackgroundMediumBrush}" Padding="8,6">
            <DockPanel>
                <!-- Right side buttons -->
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Content="Clear" Command="{Binding ClearLogCommand}"
                            Background="Transparent" Foreground="{StaticResource TextPrimaryBrush}"
                            BorderThickness="0" Padding="8,4" Cursor="Hand" Margin="4,0"/>
                    <Button Content="Refresh" Command="{Binding RefreshCommand}"
                            Background="Transparent" Foreground="{StaticResource TextPrimaryBrush}"
                            BorderThickness="0" Padding="8,4" Cursor="Hand" Margin="4,0"/>
                </StackPanel>

                <!-- Filter and level toggles -->
                <StackPanel Orientation="Horizontal">
                    <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                             Width="200" Margin="0,0,8,0"
                             Background="{StaticResource BackgroundDarkBrush}"
                             Foreground="{StaticResource TextPrimaryBrush}"
                             BorderBrush="{StaticResource BackgroundLightBrush}"
                             BorderThickness="1" Padding="6,4"
                             FontFamily="{StaticResource FontFamilyMonospace}"/>
                    <CheckBox Content="Info" IsChecked="{Binding ShowInfo}"
                              Foreground="{StaticResource TextSecondaryBrush}"
                              VerticalAlignment="Center" Margin="8,0"/>
                    <CheckBox Content="Warn" IsChecked="{Binding ShowWarnings}"
                              Foreground="#E5C07B"
                              VerticalAlignment="Center" Margin="8,0"/>
                    <CheckBox Content="Error" IsChecked="{Binding ShowErrors}"
                              Foreground="#E06C75"
                              VerticalAlignment="Center" Margin="8,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Log entries -->
        <ListBox Grid.Row="1"
                 ItemsSource="{Binding Entries}"
                 Background="Transparent"
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type core:DebugLogEntry}">
                    <TextBlock FontFamily="{StaticResource FontFamilyMonospace}"
                               FontSize="12" Padding="8,2" TextWrapping="NoWrap"
                               Foreground="{StaticResource TextSecondaryBrush}">
                        <Run Text="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss.fff}', Mode=OneTime}" Foreground="{StaticResource TextMutedBrush}"/>
                        <Run Text=" [" Foreground="{StaticResource TextMutedBrush}"/><Run Text="{Binding Source, Mode=OneTime}" Foreground="#61AFEF"/><Run Text="] " Foreground="{StaticResource TextMutedBrush}"/>
                        <Run Text="{Binding Message, Mode=OneTime}"/>
                    </TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource BackgroundLightBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <!-- Status bar -->
        <Border Grid.Row="2" Background="{StaticResource BackgroundMediumBrush}" Padding="8,4">
            <TextBlock Foreground="{StaticResource TextMutedBrush}" FontSize="11">
                <Run Text="{Binding TotalCount, Mode=OneWay}"/>
                <Run Text=" entries"/>
            </TextBlock>
        </Border>
    </Grid>
</UserControl>
