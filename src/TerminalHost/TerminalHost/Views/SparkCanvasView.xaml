<UserControl x:Class="TerminalHost.Views.SparkCanvasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <wv2:WebView2 x:Name="SparkWebView" DefaultBackgroundColor="#FF050510"/>

        <!-- Fallback overlay when WebView2 is not ready -->
        <Border x:Name="LoadingOverlay" Background="#FF050510"
                Visibility="Visible">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="✨" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                <TextBlock Text="Initializing Spark Canvas..."
                           FontSize="13" Foreground="#66ccff"
                           FontFamily="Cascadia Code,Consolas,monospace"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
