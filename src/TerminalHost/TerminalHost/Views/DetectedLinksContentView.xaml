<UserControl x:Class="TerminalHost.Views.DetectedLinksContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:TerminalHost.ViewModels"
             d:DataContext="{d:DesignInstance viewModels:DetectedLinksViewModel}"
             mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="{StaticResource BackgroundMediumBrush}" Padding="8,4">
            <DockPanel>
                <Button DockPanel.Dock="Right"
                        Content="↻"
                        Style="{StaticResource TabCloseButton}"
                        Command="{Binding RefreshLinksCommand}"
                        ToolTip="Refresh links"
                        FontSize="{StaticResource FontSizeNormal}"
                        Padding="6,2"/>
                <TextBlock Text="" />
            </DockPanel>
        </Border>

        <!-- Links List -->
        <ListBox Grid.Row="1"
                 ItemsSource="{Binding Links}"
                 SelectedItem="{Binding SelectedLink}"
                 Background="Transparent"
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="8,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="{Binding Icon}"
                                   Foreground="{StaticResource TextPrimaryBrush}"
                                   FontSize="{StaticResource FontSizeNormal}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding DisplayText}"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       FontSize="{StaticResource FontSizeSmall}"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding Url}"
                                       Foreground="{StaticResource TextMutedBrush}"
                                       FontSize="{StaticResource FontSizeMicro}"
                                       TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding Url}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem" BasedOn="{StaticResource PopupListBoxItem}"/>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <!-- Empty state -->
        <TextBlock Grid.Row="1"
                   Text="No links detected"
                   Foreground="{StaticResource TextMutedBrush}"
                   FontSize="{StaticResource FontSizeSmall}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Padding="20"
                   Visibility="{Binding IsEmptyStateVisible, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <!-- Footer -->
        <Border Grid.Row="2" Background="{StaticResource BackgroundMediumBrush}" Padding="8,4">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Preview"
                        Style="{StaticResource TerminalSwitchButton}"
                        Command="{Binding PreviewSelectedLinkCommand}"
                        ToolTip="Preview file"
                        Padding="8,4"
                        Margin="0,0,4,0"/>
                <Button Content="Open"
                        Style="{StaticResource TerminalSwitchButton}"
                        Command="{Binding OpenSelectedLinkCommand}"
                        ToolTip="Open link"
                        Padding="8,4"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
